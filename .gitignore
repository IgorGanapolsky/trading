# Environment
.env
.env.devloop
.env.local
.env.*.local
.venv/
venv/
.venv/
.pc-venv/
env/
ENV/
*.pyc
__pycache__/
*.py[cod]
*$py.class

# IDEs
.vscode/
.idea/
*.swp

# Claude Code - allow configuration but ignore state
.claude/ralph_state.json
*.swo
.DS_Store

# OMX (oh-my-codex) local UI/state (never commit)
.omx/

# Data
data/*.csv
data/*.json
# Runtime kill-switch / halt flags — local only (must never be resurrected by checkout)
data/TRADING_HALTED
data/SYSTEM_HALTED
data/trading_halt.txt
# Whitelist canonical trade data files
!data/system_state.json
!data/trades.json
!data/ic_entries.json
!data/put_credit_entries.json
!data/thumbgate_rules.json
!data/strategy_params.json
!data/ic_stats.json
!data/trade_journal.jsonl
!data/runtime/
!data/runtime/**
logs/*.log
population_log*.txt

# Web artifacts (downloaded pages, archives)
*.mht
article_*.html

# Cache directories (API responses, health logs, etc.) - ignore recursively
data/cache/
data/gpu_backtests/cache/
data/gpu_backtests/price_cache/
data/market_cache/
**/*.jsonl

# Historical data (can be regenerated, but some may be useful to track)
# Note: data/historical/ contains CSV files that may be committed for backtesting
# Uncomment the next line if you want to ignore all historical data:
# data/historical/

# Docker
*.pid

# Core dumps (prevent accidental commits from crashes).
# IMPORTANT: patterns must be root-anchored — bare "core" also matched the
# src/core/ package directory and silently blocked new strategy modules.
/core
/core.*
*.core

# Secrets
*_secret*
*_key*
api_keys.txt
.github/local-secrets.env
.secrets
!scripts/validate_env_keys.py
# Block test files in root (but allow in tests/ directory)
/test_*.py
/*_test.py

# Testing
.pytest_cache/
.coverage
htmlcov/

# Node.js (Trigger.dev)
node_modules/

# Distribution
dist/
build/
*.egg-info/

# Persistent State Management
# Allow system state to be committed for cross-machine continuity
!data/.gitkeep
!data/system_state.json
!data/tier2_watchlist.json
!data/watchlist_4ms_small_capital.json
!data/performance_log.json
!data/heartbeat.json
data/google_sheets_credentials.json

# Local CI/CD
.pc-venv/
.actrc
.cursorrules
.mypy_cache/
.ruff_cache/
.pre-commit-config.yaml.save
worktrees/
.worktrees/
trading_worktrees/
GEMINI.md

# High-churn agent state and generated artifacts
data/agent_context/
data/options_signals/
data/trading_plans/
data/evaluations/
data/sentiment/news_*.json

# Runtime artifacts and logs
*.db
manual_run*.txt
failed_log.txt
pytest_cov_report.txt
ralph_output.txt
*.trigger
LIQUIDATE_NOW
TRIGGER_SYNC_NOW
reports/*.txt
reports/**/*.json
data/audit_trail/
data/sentiment/
data/bronze/
data/silver/
data/gold/
wiki/
data/feedback/
data/backups/
data/lancedb_feedback/
data/chroma/
data/chroma_db/
.trade_lock
.envrc
.claude/memory/feedback/
.claude/memory/lancedb/
.claude/memory/model_cache/
.claude/memory/chroma_db/
.claude/memory/memalign/
.claude/memory/lessons-learned.memory.md
.claude/memory/*.json
.claude/scripts/feedback/venv/

# High-churn agent state (never commit)
.claude/ralph/
.claude/orchestrator_state.json
.claude/reports/*.txt

# CCPM local workspace (project management)
.claude/epics/
.claude/prds/
.claude/context/

# Generated workflow artifacts
artifacts/**
!artifacts/.gitkeep

# Coverage XML reports (regenerated by pytest)
coverage*.xml
coverage*.json

# Audit reports (generated artifacts)
data/audit_reports/

# Generated site (MkDocs/Jekyll output)
site/

# RAG knowledge lessons (local only - never commit)
rag_knowledge/

# Consolidated memory (local only)
.claude/memory/consolidated_memory.json

# Legacy MCP Memory Gateway local state
.rlhf/

# ThumbGate local state
.thumbgate/

# Agent-generated local state
.agents/
.codex/

# === REPO HYGIENE (2026-08-02) ===
# Runtime / agent garbage — never re-commit. Canonical ledgers stay whitelisted above.

# Screenshots, browser dumps, IDE/editor state
data/screenshots/
.playwright-mcp/
.obsidian/
.aider*
.aider.chat.history.md

# Audit / analysis / report dumps (regenerated locally)
data/audit/
data/audit_trail/
data/audit_reports/
data/analysis/
data/reports/
data/debug/
data/post_market_analysis/
data/workflow_health/
data/workflow_state/
data/swarm_state/
data/claude_ops/
data/context_engine/
data/vector_db/
data/blog_cache/
data/youtube_cache/
data/newsletter_signals/
data/external_signals/
data/market_signals/
data/options_signals/
data/sentiment/
data/evaluations/
data/feedback/
data/memory/
data/ipo/
data/archive/
data/analytics/
data/revenue/
data/research/
data/trade_plans/
data/backtests/
data/backtests_verification/
data/gpu_backtests/
data/ml_training_data/
data/datasets/
data/rag_knowledge/

# Deprecated raw daily trade dumps (use data/trades.json)
data/trades_*.json
data/trades_for_clustering.json
data/options_trades_*.json
data/backtest_trades.json

# Logs + generated workflow artifacts
logs/
!logs/.gitkeep
artifacts/
!artifacts/.gitkeep

# Claude session logs (config under .claude/ stays tracked)
.claude/logs/

# Binary ML training dumps stay local (JSON weights under models/ml may be tracked intentionally)
*.pt
!models/ml/*.pt

