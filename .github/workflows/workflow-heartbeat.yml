name: Workflow Heartbeat

on:
  schedule:
    # Run every day at 2 AM UTC (keeps workflows active)
    # This prevents GitHub from disabling workflows due to inactivity
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  heartbeat:
    name: Keep Workflows Active
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Heartbeat
        run: |
          echo "💓 Workflow Heartbeat - $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "✅ This keeps all scheduled workflows active"
          echo "✅ Prevents GitHub from disabling workflows due to inactivity"
          echo ""
          echo "Active workflows:"
          echo "  - Daily Trading Execution (9:35 AM ET weekdays)"
          echo "  - Weekend Crypto Trading"
          echo "  - YouTube Analysis"
          echo "  - All other scheduled workflows"
          echo ""
          echo "✅ Repository is active - workflows will continue running"
