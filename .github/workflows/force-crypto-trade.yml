name: Force Crypto Trade (World-Class Strategy)

on:
  push:
    paths:
      - '.github/FORCE_CRYPTO'
    branches: ['*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  crypto-trade:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install alpaca-py yfinance pandas numpy requests

      - name: Execute Crypto Trade
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
        run: |
          python3 << 'PYTHON'
          """
          World-Class Crypto Trading Strategy v4.0 (Trend-Aware)

          RESEARCH-BACKED:
          - Only buy when price > 50-day MA (5x better returns per Coinbase)
          - Fear & Greed for SIZING only, not timing
          - Stop catching falling knives
          """
          import os, sys, json, traceback, requests
          from datetime import datetime, timezone

          today = datetime.now(timezone.utc).strftime("%Y-%m-%d")
          result = {"timestamp": datetime.now(timezone.utc).isoformat(), "date": today, "strategy": "WorldClassCrypto", "strategy_version": "4.0"}

          def save():
              os.makedirs("data", exist_ok=True)
              f = f"data/trades_{today}.json"
              t = json.load(open(f)) if os.path.exists(f) else []
              if not isinstance(t, list): t = [t]
              t.append(result)
              json.dump(t, open(f, "w"), indent=2)
              print(f"✅ Saved to {f}")

          api_key, secret = os.environ.get("ALPACA_API_KEY",""), os.environ.get("ALPACA_SECRET_KEY","")
          print("=" * 60)
          print("🌍 WORLD-CLASS CRYPTO STRATEGY v4.0 (Trend-Aware)")
          print("   Only buy when price > 50-day MA")
          print("=" * 60)

          if not api_key or not secret:
              result["error"], result["status"] = "MISSING_SECRETS", "FAILED"
              save()
              sys.exit(0)

          try:
              from alpaca.trading.client import TradingClient
              from alpaca.trading.requests import MarketOrderRequest
              from alpaca.trading.enums import OrderSide, TimeInForce
              import yfinance as yf

              client = TradingClient(api_key, secret, paper=True)
              acct = client.get_account()
              result["account_value"] = float(acct.equity)
              print(f"✅ Account: ${result['account_value']:,.2f}")

              # STEP 1: TREND CHECK (50-day MA)
              print("\n📊 STEP 1: Trend Check (50-day MA)")
              print("-" * 40)
              trend_data = {}
              for s in ["BTC-USD","ETH-USD","SOL-USD"]:
                  try:
                      h = yf.Ticker(s).history(period="60d")
                      if len(h) >= 50:
                          price = float(h["Close"].iloc[-1])
                          ma50 = float(h["Close"].rolling(50).mean().iloc[-1])
                          above = price > ma50
                          pct = ((price - ma50) / ma50) * 100
                          chg = ((h["Close"].iloc[-1] - h["Close"].iloc[-7]) / h["Close"].iloc[-7]) * 100
                          trend_data[s] = {"price":price,"ma50":ma50,"above":above,"pct":pct,"chg7d":chg}
                          st = "✅ ABOVE" if above else "❌ BELOW"
                          print(f"   {s}: ${price:,.0f} | MA50: ${ma50:,.0f} | {st} ({pct:+.1f}%)")
                  except Exception as e:
                      print(f"   {s}: Error - {e}")

              result["trend_analysis"] = trend_data
              above_ma = {k:v for k,v in trend_data.items() if v.get("above")}

              if not above_ma:
                  print("\n⛔ ALL CRYPTOS BELOW 50-DAY MA - SKIPPING")
                  print("   Research: Buying below MA = catching falling knife")
                  result["action"] = "SKIP"
                  result["skip_reason"] = "All below 50-day MA - waiting for trend"
                  result["status"] = "WAITING_FOR_TREND"
                  save()
                  sys.exit(0)

              # STEP 2: Fear & Greed (for sizing only)
              print("\n📊 STEP 2: Fear & Greed (sizing only)")
              mult = 1.0
              try:
                  fng = requests.get("https://api.alternative.me/fng/", timeout=10).json()
                  fv = int(fng["data"][0]["value"])
                  result["fear_greed_index"] = fv
                  fc = fng["data"][0]["value_classification"]
                  result["fear_greed_class"] = fc
                  print(f"   Value: {fv} ({fc})")
                  if fv <= 25: mult = 1.5; print("   🟢 EXTREME FEAR + TREND = 1.5x")
                  elif fv <= 40: mult = 1.25; print("   🟢 FEAR + TREND = 1.25x")
                  elif fv <= 60: mult = 1.0; print("   🟡 NEUTRAL + TREND = 1.0x")
                  elif fv <= 75: mult = 0.75; print("   🟠 GREED + TREND = 0.75x")
                  else:
                      print("   🔴 EXTREME GREED - Skip")
                      result["action"], result["skip_reason"] = "SKIP", "Extreme greed"
                      save()
                      sys.exit(0)
              except: pass

              # STEP 3: Select best (above MA only)
              print("\n🎯 STEP 3: Select Best (above MA)")
              best_item = max(above_ma.items(), key=lambda x: x[1]["chg7d"])
              best, best_data = best_item[0].replace("-USD","USD"), best_item[1]
              print(f"   Selected: {best} (+{best_data['pct']:.1f}% above MA)")

              # STEP 4: Execute
              print("\n💰 STEP 4: Execute Trade")
              base = 10.0
              amt = min(base * mult, result["account_value"] * 0.02)
              if amt < 1: amt = 1
              print(f"   Amount: ${amt:.2f} (base ${base} × {mult}x)")

<<<<<<< HEAD
              try:
                  import yfinance as yf
                  cryptos = ["BTC-USD", "ETH-USD", "SOL-USD"]
                  analysis = {}

                  for symbol in cryptos:
                      try:
                          ticker = yf.Ticker(symbol)
                          hist = ticker.history(period="7d")
                          if not hist.empty:
                              price = float(hist["Close"].iloc[-1])
                              change = ((hist["Close"].iloc[-1] - hist["Close"].iloc[0]) / hist["Close"].iloc[0]) * 100
                              analysis[symbol] = {"price": price, "change_7d": change}
                              print(f"   {symbol}: ${price:,.2f} ({change:+.2f}% 7d)")
                      except Exception as e:
                          print(f"   {symbol}: Error - {e}")

                  if analysis:
                      # Pick best performer
                      best = max(analysis.items(), key=lambda x: x[1]["change_7d"])
                      best_symbol = best[0].replace("-USD", "USD")
                      best_price = best[1]["price"]
                      best_momentum = best[1]["change_7d"]
                      result["crypto_analysis"] = analysis

                      # MOMENTUM FILTER with EXTREME FEAR OVERRIDE
                      # In Extreme Fear (<=25), we BUY anyway - contrarian strategy
                      if best_momentum < 0:
                          if result.get("fear_greed_index", 50) <= 25:
                              print(f"\n   🔥 EXTREME FEAR OVERRIDE: Buying despite negative momentum!")
                              print(f"   Fear & Greed = {result.get('fear_greed_index')} (Extreme Fear = contrarian BUY)")
                              print(f"   {best_symbol} at {best_momentum:+.2f}% - historically oversold")
                              result["contrarian_buy"] = True
                          else:
                              print(f"\n   ⛔ SKIP: Best performer {best_symbol} has negative momentum ({best_momentum:+.2f}%)")
                              print("   All cryptos in downtrend - waiting for positive momentum")
                              result["action"] = "SKIP"
                              result["skip_reason"] = f"Negative momentum: {best_symbol} at {best_momentum:+.2f}%"
                              save_result()
                              sys.exit(0)

                      print(f"\n   🎯 Selected: {best_symbol} (7d momentum: {best_momentum:+.2f}%)")
              except Exception as e:
                  print(f"   ⚠️ yfinance failed: {e}")
                  print("   Defaulting to BTCUSD")
                  result["analysis_error"] = str(e)

              # Calculate position size
              base_amount = 10.0
              final_amount = base_amount * size_multiplier
              max_position = account_value * 0.02  # 2% max
              final_amount = min(final_amount, max_position)
              if final_amount < 1.0:
                  final_amount = 1.0

              print(f"\n💰 Position: ${final_amount:.2f} (base ${base_amount} × {size_multiplier}x)")

              # Execute trade
              print(f"\n📈 Submitting: BUY ${final_amount:.2f} {best_symbol}")
=======
>>>>>>> origin/main
              order = client.submit_order(MarketOrderRequest(
                  symbol=best, notional=amt, side=OrderSide.BUY, time_in_force=TimeInForce.GTC))
              print(f"   ✅ Order: {order.id}")

              result.update({
                  "symbol": best, "action": "BUY", "notional": amt,
                  "price": best_data["price"], "order_id": str(order.id),
                  "status": str(order.status), "size_multiplier": mult,
                  "trend_confirmed": True, "pct_above_ma": best_data["pct"]
              })

          except Exception as e:
              print(f"\n❌ ERROR: {e}")
              traceback.print_exc()
              result["error"], result["status"] = str(e), "FAILED"

          save()
          print("\n" + "=" * 60)
          print(json.dumps({k:v for k,v in result.items() if k != "trend_analysis"}, indent=2))
          PYTHON

      - name: Commit trade data
        if: always()
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "trade: crypto v4.0 (trend-aware) [skip ci]"
          file_pattern: "data/trades_*.json"
          commit_user_name: "GitHub Actions Bot"
          commit_user_email: "actions@github.com"
